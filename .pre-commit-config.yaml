repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.0
    hooks:
      - id: clang-format
        types_or: [c, c++]
        args: [--dry-run, --Werror]  # 检查模式：有差异则报错拒绝提交
        exclude: ^third_party/